language: scala
scala:
  - 2.10.3
jdk:
  - oraclejdk7
  - openjdk6
env:
  matrix:
    - MODE=Main
    - MODE=Gentests

before_script: ./travis_build.sh Compile
script: ./travis_build.sh $MODE
#In Matrix, should be executed once, placeholder for publishing
after_success:
  - echo "ROCK!"
after_failure:
  - echo "PITY!"